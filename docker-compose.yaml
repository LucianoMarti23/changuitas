version: "3.3"

services:
  laravel:
    build: .
    container_name: laravel-app
    ports:
      - "8000:8000"
    volumes:
      - .:/var/www
      - ./storage:/var/www/html/storage
    working_dir: /var/www
